const { prisma } = require("../../config/database");
const count = (model, where) => prisma[model].count({ where });
const dashboard = async () => { const [students, teachers, staff, parents, management, activeModules, settings, pendingAdmissions, outstandingFees, recentPayments, activePrefects, upcomingEvents, recentAnnouncements] = await Promise.all([count("student", { status: "ACTIVE" }), count("staff", { user: { role: "TEACHER" }, status: "ACTIVE" }), count("staff", { status: "ACTIVE" }), count("parent", {}), count("user", { role: "MANAGEMENT", status: "ACTIVE" }), prisma.schoolSetting.count({ where: { key: { startsWith: "module-access:" }, value: { contains: '"enabled":true' } } }), prisma.schoolSetting.count({ where: { key: { startsWith: "module-access:" } } }), count("admission", { status: { in: ["APPLIED", "UNDER_REVIEW"] } }), count("studentFeeAccount", { balance: { gt: 0 } }), prisma.payment.findMany({ where: { status: "PAID" }, orderBy: { paymentDate: "desc" }, take: 10 }), count("prefectAssignment", { status: "ACTIVE" }), Promise.resolve([]), prisma.announcement.findMany({ where: { published: true }, orderBy: { createdAt: "desc" }, take: 10 })]); return { students, teachers, staff, parents, management, activeModules, disabledModules: Math.max(settings - activeModules, 0), pendingAdmissions, outstandingFees, recentPayments, activePrefects, upcomingEvents, recentAnnouncements }; };
module.exports = { dashboard };
