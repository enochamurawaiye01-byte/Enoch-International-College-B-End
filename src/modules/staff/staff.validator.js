const { z } = require("zod");
const uuid = z.string().uuid("Must be a valid ID.");
const createStaffSchema = z.object({ firstName: z.string().trim().min(1).max(80), middleName: z.string().trim().max(80).nullable().optional(), lastName: z.string().trim().min(1).max(80), email: z.string().email(), phoneNumber: z.string().trim().max(30).nullable().optional(), password: z.string().min(8).max(100), staffNumber: z.string().trim().min(2).max(50), gender: z.enum(["MALE", "FEMALE", "OTHER"]).nullable().optional(), dateOfBirth: z.coerce.date().nullable().optional(), departmentId: uuid.nullable().optional(), jobTitle: z.string().trim().max(120).nullable().optional(), employmentType: z.enum(["FULL_TIME", "PART_TIME", "CONTRACT", "TEMPORARY"]).optional(), status: z.enum(["ACTIVE", "INACTIVE", "SUSPENDED", "RESIGNED", "TERMINATED", "RETIRED"]).optional(), employmentDate: z.coerce.date().nullable().optional(), qualification: z.string().trim().max(300).nullable().optional(), address: z.string().trim().max(500).nullable().optional() }).strict();
const updateStaffSchema = createStaffSchema.omit({ password: true, email: true, staffNumber: true }).partial().strict().refine((data) => Object.keys(data).length > 0, "At least one field is required.");
module.exports = { createStaffSchema, updateStaffSchema };
